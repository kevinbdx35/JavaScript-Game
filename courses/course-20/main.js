//Use the same template of index.html that before like in course-1

(function (firstName) {
  ///Block code
  console.log("Welcome " + firstName);
})("Laurence");

let result = (function () {
  return "Hello World";
});
console.log(result());
